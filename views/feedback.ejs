<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Feedback - Dawn Digital Technology</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <script src="/search.js"></script>
    <link href="index.css"  rel="stylesheet" >
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #fffaf0;
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .feedback-container {
            background: #ffcc80;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.2);
            width: 400px;
            text-align: center;
        }
        .feedback-container h2 {
            color: #e65100;
        }
        .feedback-container input,.feedback-container textarea,.feedback-container select {
            width: 100%;
            padding: 12px;
            margin-top: 10px;
            border: 2px solid #e65100;
            border-radius: 5px;
            font-size: 16px;
            background: #fff;
            transition: all 0.3s ease-in-out;
        }
        .feedback-container input:focus,.feedback-container textarea:focus,.feedback-container select:focus {
            border-color: #ff9800;
            box-shadow: 0 0 10px rgba(255, 152, 0, 0.5);
            outline: none;
        }
        .feedback-container select {
            appearance: none;
            background: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="black"><path d="M7 10l5 5 5-5z"/></svg>') no-repeat right 10px center;
            background-size: 20px;
        }
        .feedback-container button {
            background: #e65100;
            color: white;
            padding: 12px;
            border: none;
            border-radius: 5px;
            margin-top: 15px;
            cursor: pointer;
            font-size: 16px;
            transition: 0.3s;
        }
        .feedback-container button:hover {
            background: #ff9800;
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>
    <div id="floatingSidebar" class="fixed top-1/3 right-0 transform translate-x-full transition-transform duration-500 z-50 bg-white shadow-2xl rounded-l-2xl w-72 p-4 border border-gray-200" 
    onmouseenter="clearHideTimeout()" onmouseleave="startHideTimeout()">
    
    <!-- Close Button -->
    <div class="absolute top-2 left-2 cursor-pointer text-gray-600" onclick="toggleSidebar()">✖</div>
  
    <!-- Content -->
    <h2 class="text-xl font-bold mb-4 text-gray-800">Our Pillars</h2>
    <ul class="space-y-3 text-gray-700">
      <li><strong>Methodology:</strong> A structured, agile-driven approach to solve modern challenges.</li>
      <li><strong>Philosophy:</strong> Ethical, client-centered innovation with impactful results.</li>
      <li><strong>The Way We Think:</strong> Forward-thinking strategies, anticipating future tech waves.</li>
      <li><strong>Reinvention & AI-First:</strong> AI-driven solutions to transform businesses from the ground up.</li>
    </ul>
  </div>
  
  <!-- Floating Handle (Button to Open Sidebar) -->
  <div id="floatingHandle" class="fixed top-1/3 right-0 bg-orange-500 text-white rounded-l-full px-3 py-2 cursor-pointer shadow-lg z-50" onclick="toggleSidebar()">
    <span>☰ Our Pillars</span>
  </div>
  <div class="feedback-container" data-aos="zoom-in">
      <h2>Feedback Form</h2>
      <form id="feedbackForm">
          <input type="text" name="name" id="name" placeholder="Your Name" required data-aos="fade-right">
          <input type="email" name="email" id="email" placeholder="Your Email" required data-aos="fade-left">
          <select name="rating" id="rating" required data-aos="fade-up">
              <option value="">Rate Us</option>
              <option value="5">⭐️⭐️⭐️⭐️⭐️ (Excellent)</option>
              <option value="4">⭐️⭐️⭐️⭐️ (Good)</option>
              <option value="3">⭐️⭐️⭐️ (Average)</option>
              <option value="2">⭐️⭐️ (Poor)</option>
              <option value="1">⭐️ (Bad)</option>
          </select>
          <textarea name="message" id="message" placeholder="Your Feedback" rows="4" required data-aos="fade-down"></textarea>
          <button type="submit" data-aos="flip-left">Submit</button>
      </form>
  </div>
  <script>
      AOS.init();

      document.getElementById("feedbackForm").addEventListener("submit", function(event) {
          event.preventDefault(); // Prevent form submission

          let name = document.getElementById("name").value.trim();
          let email = document.getElementById("email").value.trim();
          let rating = document.getElementById("rating").value;
          let message = document.getElementById("message").value.trim();

          if (!name || !email || !rating || !message) {
              Swal.fire({
                  icon: 'warning',
                  title: 'Oops...',
                  text: 'Please fill in all the required fields!',
              });
              return;
          }

          Swal.fire({
              icon: 'success',
              title: 'Thank You!',
              text: 'Your feedback has been submitted successfully!',
              confirmButtonColor: '#e65100'
          });

          document.getElementById("feedbackForm").reset();
      });
  </script>
</body>
</html>